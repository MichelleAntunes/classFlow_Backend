// export enum  {
// 	por enquanto não há nenhum enum neste projeto
// }

export type TStudents = {
  id: string;
  name: string;
  email: string;
  age: number;
  telepfone: number;
  notes: string;
};
